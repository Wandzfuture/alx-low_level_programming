#include "main.h"

/**
 * _islower - prints 1 or 0 depending on the input.
 * @c: first param.
 *
 * Description: prints all The lowercase letters.
 * Return: (0(Always)).
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
